#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
 * _strdup - function returns a pointer
 * @str: the string
 * Return: pointer
 */

char *_strdup(char *str)
{
	char *ptr = malloc(sizeof(char) * strlen(str));

	strcpy(ptr, str);
	return ptr;
}
