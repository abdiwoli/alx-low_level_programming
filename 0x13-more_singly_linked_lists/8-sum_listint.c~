#include "lists.h"
/**
 * listint_len - returns num of elements
 * @h: linked list
 * Return: num
 */
size_t listint_len(const listint_t *h)
{
	int num = 0;

	while (h)
	{
		num++;
		h = h->next;
	}
	return (num);
}
