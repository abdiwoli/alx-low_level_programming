#include "main.h"
/**
 * rev_string - reverses
 * @s: string pram
 */

void rev_string(char *s)
{
	char st[100], *str;
	while (*s != '\0')
		s++;
	s--;
	int n;

	n = 0;
	while (s > =str)
	{
		st[n++] = *s;
		s--;
	}
	st[n] = '\0';
	*s = st;
}