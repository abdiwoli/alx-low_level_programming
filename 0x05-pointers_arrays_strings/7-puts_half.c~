#include "main.h"
/**
 * puts_half - puts half string
 * @str: string param
 */

void puts_half(char *str)
{
	int i, n;
	char *st;

	i = 0;
	st = str;
	while (*st != '\0')
	{
		i++;
		st++;
	}
	i = i/2;

	for (n = 0; n < i; n++)
		_putchar(str[i]);
	_putchar('\n');
	
}
