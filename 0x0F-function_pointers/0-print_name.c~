#include "function_pointers.h"
#include <stdio.h>

/**
 * print_name - function prints name
 * @name: the name
 * @(*f)(char *): function
 */

void print_name(char *name, void (*f)(char *))
{
	f(name);
}
